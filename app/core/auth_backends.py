# from django.contrib.auth.backends import BaseBackend

# """ custon authentcation backend"""
# class UserAuthBackend(BaseBackend):
#     """Email or phone number Authentication Backend
    
#     Allow user sign in with email or phone number
#     then check password is valid or not 
#     and return user else None

#     """
#     def authenticate(self, request,email_or_phone=None, password=None, role=None):
#         try:
#             user = U
#         return super().authenticate(request, **kwargs)